-# <サイドバー>
.Side-bar
  .Side-bar__user-name
    .Top-items
      .Top-items__name
        = current_user.name
      %ul.Top-items__icons
        %li
          = link_to "#" do
            = icon('far', 'edit', class:"icon")
        %li
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class:"icon")
  .Side-bar__group-list
    .Chat-group
      = link_to "#", class:'Chat-group__a' do
        .Chat-group__name
          gurop名
        .Chat-group__message
          まだメッセージがありません
    .Chat-group
      = link_to "#", class:'Chat-group__a' do
        .Chat-group__name
          gurop名
        .Chat-group__message
          まだメッセージがありません